{
  "name": "forgma-plugin",
  "private": true,
  "version": "0.0.1",
  "author": "John Dunning",
  "description": "A Figma plugin for generating forms in Form.io",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "plugin:build": "npm run build -w plugin",
    "plugin:dev": "npm run dev -w plugin"
  }
}
